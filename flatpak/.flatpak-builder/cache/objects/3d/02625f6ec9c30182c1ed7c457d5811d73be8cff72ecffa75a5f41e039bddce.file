/*
 * Generated by gdbus-codegen 2.58.1 from org.gnome.evolution.dataserver.Subprocess.Backend.xml. DO NOT EDIT.
 *
 * The license of this code is the same as for the D-Bus interface description
 * it was derived from.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "e-dbus-subprocess-backend.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  gboolean use_gvariant;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface org.gnome.evolution.dataserver.Subprocess.Backend
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:EDBusSubprocessBackend
 * @title: EDBusSubprocessBackend
 * @short_description: Generated C code for the org.gnome.evolution.dataserver.Subprocess.Backend D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-gnome-evolution-dataserver-Subprocess-Backend.top_of_page">org.gnome.evolution.dataserver.Subprocess.Backend</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.gnome.evolution.dataserver.Subprocess.Backend ---- */

static const _ExtendedGDBusArgInfo _e_dbus_subprocess_backend_method_info_create_IN_ARG_source_id =
{
  {
    -1,
    (gchar *) "source_id",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _e_dbus_subprocess_backend_method_info_create_IN_ARG_backend_factory_type_name =
{
  {
    -1,
    (gchar *) "backend_factory_type_name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _e_dbus_subprocess_backend_method_info_create_IN_ARG_module_filename =
{
  {
    -1,
    (gchar *) "module_filename",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _e_dbus_subprocess_backend_method_info_create_IN_ARG_pointers[] =
{
  &_e_dbus_subprocess_backend_method_info_create_IN_ARG_source_id.parent_struct,
  &_e_dbus_subprocess_backend_method_info_create_IN_ARG_backend_factory_type_name.parent_struct,
  &_e_dbus_subprocess_backend_method_info_create_IN_ARG_module_filename.parent_struct,
  NULL
};

static const _ExtendedGDBusArgInfo _e_dbus_subprocess_backend_method_info_create_OUT_ARG_object_path =
{
  {
    -1,
    (gchar *) "object_path",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _e_dbus_subprocess_backend_method_info_create_OUT_ARG_pointers[] =
{
  &_e_dbus_subprocess_backend_method_info_create_OUT_ARG_object_path.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _e_dbus_subprocess_backend_method_info_create =
{
  {
    -1,
    (gchar *) "Create",
    (GDBusArgInfo **) &_e_dbus_subprocess_backend_method_info_create_IN_ARG_pointers,
    (GDBusArgInfo **) &_e_dbus_subprocess_backend_method_info_create_OUT_ARG_pointers,
    NULL
  },
  "handle-create",
  FALSE
};

static const _ExtendedGDBusMethodInfo _e_dbus_subprocess_backend_method_info_close =
{
  {
    -1,
    (gchar *) "Close",
    NULL,
    NULL,
    NULL
  },
  "handle-close",
  FALSE
};

static const GDBusMethodInfo * const _e_dbus_subprocess_backend_method_info_pointers[] =
{
  &_e_dbus_subprocess_backend_method_info_create.parent_struct,
  &_e_dbus_subprocess_backend_method_info_close.parent_struct,
  NULL
};

static const _ExtendedGDBusArgInfo _e_dbus_subprocess_backend_signal_info_backend_closed_ARG_sender =
{
  {
    -1,
    (gchar *) "sender",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _e_dbus_subprocess_backend_signal_info_backend_closed_ARG_pointers[] =
{
  &_e_dbus_subprocess_backend_signal_info_backend_closed_ARG_sender.parent_struct,
  NULL
};

static const _ExtendedGDBusSignalInfo _e_dbus_subprocess_backend_signal_info_backend_closed =
{
  {
    -1,
    (gchar *) "BackendClosed",
    (GDBusArgInfo **) &_e_dbus_subprocess_backend_signal_info_backend_closed_ARG_pointers,
    NULL
  },
  "backend-closed"
};

static const GDBusSignalInfo * const _e_dbus_subprocess_backend_signal_info_pointers[] =
{
  &_e_dbus_subprocess_backend_signal_info_backend_closed.parent_struct,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _e_dbus_subprocess_backend_interface_info =
{
  {
    -1,
    (gchar *) "org.gnome.evolution.dataserver.Subprocess.Backend",
    (GDBusMethodInfo **) &_e_dbus_subprocess_backend_method_info_pointers,
    (GDBusSignalInfo **) &_e_dbus_subprocess_backend_signal_info_pointers,
    NULL,
    NULL
  },
  "backend",
};


/**
 * e_dbus_subprocess_backend_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-gnome-evolution-dataserver-Subprocess-Backend.top_of_page">org.gnome.evolution.dataserver.Subprocess.Backend</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 *
 * Since: 3.16
 */
GDBusInterfaceInfo *
e_dbus_subprocess_backend_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_e_dbus_subprocess_backend_interface_info.parent_struct;
}

/**
 * e_dbus_subprocess_backend_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #EDBusSubprocessBackend interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 *
 * Since: 3.16
 */
guint
e_dbus_subprocess_backend_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * EDBusSubprocessBackend:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-gnome-evolution-dataserver-Subprocess-Backend.top_of_page">org.gnome.evolution.dataserver.Subprocess.Backend</link>.
 *
 * Since: 3.16
 */

/**
 * EDBusSubprocessBackendIface:
 * @parent_iface: The parent interface.
 * @handle_close: Handler for the #EDBusSubprocessBackend::handle-close signal.
 * @handle_create: Handler for the #EDBusSubprocessBackend::handle-create signal.
 * @backend_closed: Handler for the #EDBusSubprocessBackend::backend-closed signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-gnome-evolution-dataserver-Subprocess-Backend.top_of_page">org.gnome.evolution.dataserver.Subprocess.Backend</link>.
 *
 * Since: 3.16
 */

typedef EDBusSubprocessBackendIface EDBusSubprocessBackendInterface;
G_DEFINE_INTERFACE (EDBusSubprocessBackend, e_dbus_subprocess_backend, G_TYPE_OBJECT)

static void
e_dbus_subprocess_backend_default_init (EDBusSubprocessBackendIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * EDBusSubprocessBackend::handle-create:
   * @object: A #EDBusSubprocessBackend.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_source_id: Argument passed by remote caller.
   * @arg_backend_factory_type_name: Argument passed by remote caller.
   * @arg_module_filename: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-gnome-evolution-dataserver-Subprocess-Backend.Create">Create()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call e_dbus_subprocess_backend_complete_create() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   *
   * Since: 3.16
   */
  g_signal_new ("handle-create",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (EDBusSubprocessBackendIface, handle_create),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    4,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_STRING, G_TYPE_STRING);

  /**
   * EDBusSubprocessBackend::handle-close:
   * @object: A #EDBusSubprocessBackend.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-gnome-evolution-dataserver-Subprocess-Backend.Close">Close()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call e_dbus_subprocess_backend_complete_close() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   *
   * Since: 3.16
   */
  g_signal_new ("handle-close",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (EDBusSubprocessBackendIface, handle_close),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /* GObject signals for received D-Bus signals: */
  /**
   * EDBusSubprocessBackend::backend-closed:
   * @object: A #EDBusSubprocessBackend.
   * @arg_sender: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-gnome-evolution-dataserver-Subprocess-Backend.BackendClosed">"BackendClosed"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   *
   * Since: 3.16
   */
  g_signal_new ("backend-closed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (EDBusSubprocessBackendIface, backend_closed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_STRING);

}

/**
 * e_dbus_subprocess_backend_emit_backend_closed:
 * @object: A #EDBusSubprocessBackend.
 * @arg_sender: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-gnome-evolution-dataserver-Subprocess-Backend.BackendClosed">"BackendClosed"</link> D-Bus signal.
 *
 * Since: 3.16
 */
void
e_dbus_subprocess_backend_emit_backend_closed (
    EDBusSubprocessBackend *object,
    const gchar *arg_sender)
{
  g_signal_emit_by_name (object, "backend-closed", arg_sender);
}

/**
 * e_dbus_subprocess_backend_call_create:
 * @proxy: A #EDBusSubprocessBackendProxy.
 * @arg_source_id: Argument to pass with the method invocation.
 * @arg_backend_factory_type_name: Argument to pass with the method invocation.
 * @arg_module_filename: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-gnome-evolution-dataserver-Subprocess-Backend.Create">Create()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call e_dbus_subprocess_backend_call_create_finish() to get the result of the operation.
 *
 * See e_dbus_subprocess_backend_call_create_sync() for the synchronous, blocking version of this method.
 *
 * Since: 3.16
 */
void
e_dbus_subprocess_backend_call_create (
    EDBusSubprocessBackend *proxy,
    const gchar *arg_source_id,
    const gchar *arg_backend_factory_type_name,
    const gchar *arg_module_filename,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Create",
    g_variant_new ("(sss)",
                   arg_source_id,
                   arg_backend_factory_type_name,
                   arg_module_filename),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * e_dbus_subprocess_backend_call_create_finish:
 * @proxy: A #EDBusSubprocessBackendProxy.
 * @out_object_path: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to e_dbus_subprocess_backend_call_create().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with e_dbus_subprocess_backend_call_create().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 *
 * Since: 3.16
 */
gboolean
e_dbus_subprocess_backend_call_create_finish (
    EDBusSubprocessBackend *proxy,
    gchar **out_object_path,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_object_path);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * e_dbus_subprocess_backend_call_create_sync:
 * @proxy: A #EDBusSubprocessBackendProxy.
 * @arg_source_id: Argument to pass with the method invocation.
 * @arg_backend_factory_type_name: Argument to pass with the method invocation.
 * @arg_module_filename: Argument to pass with the method invocation.
 * @out_object_path: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-gnome-evolution-dataserver-Subprocess-Backend.Create">Create()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See e_dbus_subprocess_backend_call_create() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 *
 * Since: 3.16
 */
gboolean
e_dbus_subprocess_backend_call_create_sync (
    EDBusSubprocessBackend *proxy,
    const gchar *arg_source_id,
    const gchar *arg_backend_factory_type_name,
    const gchar *arg_module_filename,
    gchar **out_object_path,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Create",
    g_variant_new ("(sss)",
                   arg_source_id,
                   arg_backend_factory_type_name,
                   arg_module_filename),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_object_path);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * e_dbus_subprocess_backend_call_close:
 * @proxy: A #EDBusSubprocessBackendProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-gnome-evolution-dataserver-Subprocess-Backend.Close">Close()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call e_dbus_subprocess_backend_call_close_finish() to get the result of the operation.
 *
 * See e_dbus_subprocess_backend_call_close_sync() for the synchronous, blocking version of this method.
 *
 * Since: 3.16
 */
void
e_dbus_subprocess_backend_call_close (
    EDBusSubprocessBackend *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Close",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * e_dbus_subprocess_backend_call_close_finish:
 * @proxy: A #EDBusSubprocessBackendProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to e_dbus_subprocess_backend_call_close().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with e_dbus_subprocess_backend_call_close().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 *
 * Since: 3.16
 */
gboolean
e_dbus_subprocess_backend_call_close_finish (
    EDBusSubprocessBackend *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * e_dbus_subprocess_backend_call_close_sync:
 * @proxy: A #EDBusSubprocessBackendProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-gnome-evolution-dataserver-Subprocess-Backend.Close">Close()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See e_dbus_subprocess_backend_call_close() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 *
 * Since: 3.16
 */
gboolean
e_dbus_subprocess_backend_call_close_sync (
    EDBusSubprocessBackend *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Close",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * e_dbus_subprocess_backend_complete_create:
 * @object: A #EDBusSubprocessBackend.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @object_path: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-gnome-evolution-dataserver-Subprocess-Backend.Create">Create()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 *
 * Since: 3.16
 */
void
e_dbus_subprocess_backend_complete_create (
    EDBusSubprocessBackend *object,
    GDBusMethodInvocation *invocation,
    const gchar *object_path)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(s)",
                   object_path));
}

/**
 * e_dbus_subprocess_backend_complete_close:
 * @object: A #EDBusSubprocessBackend.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-gnome-evolution-dataserver-Subprocess-Backend.Close">Close()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 *
 * Since: 3.16
 */
void
e_dbus_subprocess_backend_complete_close (
    EDBusSubprocessBackend *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * EDBusSubprocessBackendProxy:
 *
 * The #EDBusSubprocessBackendProxy structure contains only private data and should only be accessed using the provided API.
 *
 * Since: 3.16
 */

/**
 * EDBusSubprocessBackendProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #EDBusSubprocessBackendProxy.
 *
 * Since: 3.16
 */

struct _EDBusSubprocessBackendProxyPrivate
{
  GData *qdata;
};

static void e_dbus_subprocess_backend_proxy_iface_init (EDBusSubprocessBackendIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (EDBusSubprocessBackendProxy, e_dbus_subprocess_backend_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (EDBusSubprocessBackendProxy)
                         G_IMPLEMENT_INTERFACE (E_DBUS_SUBPROCESS_TYPE_BACKEND, e_dbus_subprocess_backend_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (EDBusSubprocessBackendProxy, e_dbus_subprocess_backend_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (E_DBUS_SUBPROCESS_TYPE_BACKEND, e_dbus_subprocess_backend_proxy_iface_init))

#endif
static void
e_dbus_subprocess_backend_proxy_finalize (GObject *object)
{
  EDBusSubprocessBackendProxy *proxy = E_DBUS_SUBPROCESS_BACKEND_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (e_dbus_subprocess_backend_proxy_parent_class)->finalize (object);
}

static void
e_dbus_subprocess_backend_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
e_dbus_subprocess_backend_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
e_dbus_subprocess_backend_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_e_dbus_subprocess_backend_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], E_DBUS_SUBPROCESS_TYPE_BACKEND);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, E_DBUS_SUBPROCESS_TYPE_BACKEND);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
e_dbus_subprocess_backend_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  EDBusSubprocessBackendProxy *proxy = E_DBUS_SUBPROCESS_BACKEND_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_e_dbus_subprocess_backend_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_e_dbus_subprocess_backend_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
e_dbus_subprocess_backend_proxy_init (EDBusSubprocessBackendProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = e_dbus_subprocess_backend_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, E_DBUS_SUBPROCESS_TYPE_BACKEND_PROXY, EDBusSubprocessBackendProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), e_dbus_subprocess_backend_interface_info ());
}

static void
e_dbus_subprocess_backend_proxy_class_init (EDBusSubprocessBackendProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = e_dbus_subprocess_backend_proxy_finalize;
  gobject_class->get_property = e_dbus_subprocess_backend_proxy_get_property;
  gobject_class->set_property = e_dbus_subprocess_backend_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = e_dbus_subprocess_backend_proxy_g_signal;
  proxy_class->g_properties_changed = e_dbus_subprocess_backend_proxy_g_properties_changed;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (EDBusSubprocessBackendProxyPrivate));
#endif
}

static void
e_dbus_subprocess_backend_proxy_iface_init (EDBusSubprocessBackendIface *iface)
{
}

/**
 * e_dbus_subprocess_backend_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-gnome-evolution-dataserver-Subprocess-Backend.top_of_page">org.gnome.evolution.dataserver.Subprocess.Backend</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call e_dbus_subprocess_backend_proxy_new_finish() to get the result of the operation.
 *
 * See e_dbus_subprocess_backend_proxy_new_sync() for the synchronous, blocking version of this constructor.
 *
 * Since: 3.16
 */
void
e_dbus_subprocess_backend_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (E_DBUS_SUBPROCESS_TYPE_BACKEND_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.gnome.evolution.dataserver.Subprocess.Backend", NULL);
}

/**
 * e_dbus_subprocess_backend_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to e_dbus_subprocess_backend_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with e_dbus_subprocess_backend_proxy_new().
 *
 * Returns: (transfer full) (type EDBusSubprocessBackendProxy): The constructed proxy object or %NULL if @error is set.
 *
 * Since: 3.16
 */
EDBusSubprocessBackend *
e_dbus_subprocess_backend_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return E_DBUS_SUBPROCESS_BACKEND (ret);
  else
    return NULL;
}

/**
 * e_dbus_subprocess_backend_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-gnome-evolution-dataserver-Subprocess-Backend.top_of_page">org.gnome.evolution.dataserver.Subprocess.Backend</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See e_dbus_subprocess_backend_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type EDBusSubprocessBackendProxy): The constructed proxy object or %NULL if @error is set.
 *
 * Since: 3.16
 */
EDBusSubprocessBackend *
e_dbus_subprocess_backend_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (E_DBUS_SUBPROCESS_TYPE_BACKEND_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.gnome.evolution.dataserver.Subprocess.Backend", NULL);
  if (ret != NULL)
    return E_DBUS_SUBPROCESS_BACKEND (ret);
  else
    return NULL;
}


/**
 * e_dbus_subprocess_backend_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like e_dbus_subprocess_backend_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call e_dbus_subprocess_backend_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See e_dbus_subprocess_backend_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 *
 * Since: 3.16
 */
void
e_dbus_subprocess_backend_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (E_DBUS_SUBPROCESS_TYPE_BACKEND_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.gnome.evolution.dataserver.Subprocess.Backend", NULL);
}

/**
 * e_dbus_subprocess_backend_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to e_dbus_subprocess_backend_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with e_dbus_subprocess_backend_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type EDBusSubprocessBackendProxy): The constructed proxy object or %NULL if @error is set.
 *
 * Since: 3.16
 */
EDBusSubprocessBackend *
e_dbus_subprocess_backend_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return E_DBUS_SUBPROCESS_BACKEND (ret);
  else
    return NULL;
}

/**
 * e_dbus_subprocess_backend_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like e_dbus_subprocess_backend_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See e_dbus_subprocess_backend_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type EDBusSubprocessBackendProxy): The constructed proxy object or %NULL if @error is set.
 *
 * Since: 3.16
 */
EDBusSubprocessBackend *
e_dbus_subprocess_backend_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (E_DBUS_SUBPROCESS_TYPE_BACKEND_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.gnome.evolution.dataserver.Subprocess.Backend", NULL);
  if (ret != NULL)
    return E_DBUS_SUBPROCESS_BACKEND (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * EDBusSubprocessBackendSkeleton:
 *
 * The #EDBusSubprocessBackendSkeleton structure contains only private data and should only be accessed using the provided API.
 *
 * Since: 3.16
 */

/**
 * EDBusSubprocessBackendSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #EDBusSubprocessBackendSkeleton.
 *
 * Since: 3.16
 */

struct _EDBusSubprocessBackendSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_e_dbus_subprocess_backend_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  EDBusSubprocessBackendSkeleton *skeleton = E_DBUS_SUBPROCESS_BACKEND_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], E_DBUS_SUBPROCESS_TYPE_BACKEND);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, E_DBUS_SUBPROCESS_TYPE_BACKEND);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_e_dbus_subprocess_backend_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  EDBusSubprocessBackendSkeleton *skeleton = E_DBUS_SUBPROCESS_BACKEND_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_e_dbus_subprocess_backend_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_e_dbus_subprocess_backend_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  EDBusSubprocessBackendSkeleton *skeleton = E_DBUS_SUBPROCESS_BACKEND_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_e_dbus_subprocess_backend_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _e_dbus_subprocess_backend_skeleton_vtable =
{
  _e_dbus_subprocess_backend_skeleton_handle_method_call,
  _e_dbus_subprocess_backend_skeleton_handle_get_property,
  _e_dbus_subprocess_backend_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
e_dbus_subprocess_backend_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return e_dbus_subprocess_backend_interface_info ();
}

static GDBusInterfaceVTable *
e_dbus_subprocess_backend_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_e_dbus_subprocess_backend_skeleton_vtable;
}

static GVariant *
e_dbus_subprocess_backend_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  EDBusSubprocessBackendSkeleton *skeleton = E_DBUS_SUBPROCESS_BACKEND_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_e_dbus_subprocess_backend_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _e_dbus_subprocess_backend_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _e_dbus_subprocess_backend_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _e_dbus_subprocess_backend_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.gnome.evolution.dataserver.Subprocess.Backend", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
e_dbus_subprocess_backend_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void
_e_dbus_subprocess_backend_on_signal_backend_closed (
    EDBusSubprocessBackend *object,
    const gchar *arg_sender)
{
  EDBusSubprocessBackendSkeleton *skeleton = E_DBUS_SUBPROCESS_BACKEND_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(s)",
                   arg_sender));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.gnome.evolution.dataserver.Subprocess.Backend", "BackendClosed",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void e_dbus_subprocess_backend_skeleton_iface_init (EDBusSubprocessBackendIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (EDBusSubprocessBackendSkeleton, e_dbus_subprocess_backend_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (EDBusSubprocessBackendSkeleton)
                         G_IMPLEMENT_INTERFACE (E_DBUS_SUBPROCESS_TYPE_BACKEND, e_dbus_subprocess_backend_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (EDBusSubprocessBackendSkeleton, e_dbus_subprocess_backend_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (E_DBUS_SUBPROCESS_TYPE_BACKEND, e_dbus_subprocess_backend_skeleton_iface_init))

#endif
static void
e_dbus_subprocess_backend_skeleton_finalize (GObject *object)
{
  EDBusSubprocessBackendSkeleton *skeleton = E_DBUS_SUBPROCESS_BACKEND_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (e_dbus_subprocess_backend_skeleton_parent_class)->finalize (object);
}

static void
e_dbus_subprocess_backend_skeleton_init (EDBusSubprocessBackendSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = e_dbus_subprocess_backend_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, E_DBUS_SUBPROCESS_TYPE_BACKEND_SKELETON, EDBusSubprocessBackendSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
e_dbus_subprocess_backend_skeleton_class_init (EDBusSubprocessBackendSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = e_dbus_subprocess_backend_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = e_dbus_subprocess_backend_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = e_dbus_subprocess_backend_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = e_dbus_subprocess_backend_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = e_dbus_subprocess_backend_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (EDBusSubprocessBackendSkeletonPrivate));
#endif
}

static void
e_dbus_subprocess_backend_skeleton_iface_init (EDBusSubprocessBackendIface *iface)
{
  iface->backend_closed = _e_dbus_subprocess_backend_on_signal_backend_closed;
}

/**
 * e_dbus_subprocess_backend_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-gnome-evolution-dataserver-Subprocess-Backend.top_of_page">org.gnome.evolution.dataserver.Subprocess.Backend</link>.
 *
 * Returns: (transfer full) (type EDBusSubprocessBackendSkeleton): The skeleton object.
 *
 * Since: 3.16
 */
EDBusSubprocessBackend *
e_dbus_subprocess_backend_skeleton_new (void)
{
  return E_DBUS_SUBPROCESS_BACKEND (g_object_new (E_DBUS_SUBPROCESS_TYPE_BACKEND_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for Object, ObjectProxy and ObjectSkeleton
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:EDBusSubprocessObject
 * @title: EDBusSubprocessObject
 * @short_description: Specialized GDBusObject types
 *
 * This section contains the #EDBusSubprocessObject, #EDBusSubprocessObjectProxy, and #EDBusSubprocessObjectSkeleton types which make it easier to work with objects implementing generated types for D-Bus interfaces.
 */

/**
 * EDBusSubprocessObject:
 *
 * The #EDBusSubprocessObject type is a specialized container of interfaces.
 */

/**
 * EDBusSubprocessObjectIface:
 * @parent_iface: The parent interface.
 *
 * Virtual table for the #EDBusSubprocessObject interface.
 */

typedef EDBusSubprocessObjectIface EDBusSubprocessObjectInterface;
G_DEFINE_INTERFACE_WITH_CODE (EDBusSubprocessObject, e_dbus_subprocess_object, G_TYPE_OBJECT, g_type_interface_add_prerequisite (g_define_type_id, G_TYPE_DBUS_OBJECT);)

static void
e_dbus_subprocess_object_default_init (EDBusSubprocessObjectIface *iface)
{
  /**
   * EDBusSubprocessObject:backend:
   *
   * The #EDBusSubprocessBackend instance corresponding to the D-Bus interface <link linkend="gdbus-interface-org-gnome-evolution-dataserver-Subprocess-Backend.top_of_page">org.gnome.evolution.dataserver.Subprocess.Backend</link>, if any.
   *
   * Connect to the #GObject::notify signal to get informed of property changes.
   *
   * Since: 3.16
   */
  g_object_interface_install_property (iface, g_param_spec_object ("backend", "backend", "backend", E_DBUS_SUBPROCESS_TYPE_BACKEND, G_PARAM_READWRITE|G_PARAM_STATIC_STRINGS));

}

/**
 * e_dbus_subprocess_object_get_backend:
 * @object: A #EDBusSubprocessObject.
 *
 * Gets the #EDBusSubprocessBackend instance for the D-Bus interface <link linkend="gdbus-interface-org-gnome-evolution-dataserver-Subprocess-Backend.top_of_page">org.gnome.evolution.dataserver.Subprocess.Backend</link> on @object, if any.
 *
 * Returns: (transfer full): A #EDBusSubprocessBackend that must be freed with g_object_unref() or %NULL if @object does not implement the interface.
 *
 * Since: 3.16
 */
EDBusSubprocessBackend *e_dbus_subprocess_object_get_backend (EDBusSubprocessObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.gnome.evolution.dataserver.Subprocess.Backend");
  if (ret == NULL)
    return NULL;
  return E_DBUS_SUBPROCESS_BACKEND (ret);
}


/**
 * e_dbus_subprocess_object_peek_backend: (skip)
 * @object: A #EDBusSubprocessObject.
 *
 * Like e_dbus_subprocess_object_get_backend() but doesn't increase the reference count on the returned object.
 *
 * <warning>It is not safe to use the returned object if you are on another thread than the one where the #GDBusObjectManagerClient or #GDBusObjectManagerServer for @object is running.</warning>
 *
 * Returns: (transfer none): A #EDBusSubprocessBackend or %NULL if @object does not implement the interface. Do not free the returned object, it is owned by @object.
 *
 * Since: 3.16
 */
EDBusSubprocessBackend *e_dbus_subprocess_object_peek_backend (EDBusSubprocessObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.gnome.evolution.dataserver.Subprocess.Backend");
  if (ret == NULL)
    return NULL;
  g_object_unref (ret);
  return E_DBUS_SUBPROCESS_BACKEND (ret);
}


static void
e_dbus_subprocess_object_notify (GDBusObject *object, GDBusInterface *interface)
{
  _ExtendedGDBusInterfaceInfo *info = (_ExtendedGDBusInterfaceInfo *) g_dbus_interface_get_info (interface);
  /* info can be NULL if the other end is using a D-Bus interface we don't know
   * anything about, for example old generated code in this process talking to
   * newer generated code in the other process. */
  if (info != NULL)
    g_object_notify (G_OBJECT (object), info->hyphen_name);
}

/**
 * EDBusSubprocessObjectProxy:
 *
 * The #EDBusSubprocessObjectProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * EDBusSubprocessObjectProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #EDBusSubprocessObjectProxy.
 */

static void
e_dbus_subprocess_object_proxy__e_dbus_subprocess_object_iface_init (EDBusSubprocessObjectIface *iface G_GNUC_UNUSED)
{
}

static void
e_dbus_subprocess_object_proxy__g_dbus_object_iface_init (GDBusObjectIface *iface)
{
  iface->interface_added = e_dbus_subprocess_object_notify;
  iface->interface_removed = e_dbus_subprocess_object_notify;
}


G_DEFINE_TYPE_WITH_CODE (EDBusSubprocessObjectProxy, e_dbus_subprocess_object_proxy, G_TYPE_DBUS_OBJECT_PROXY,
                         G_IMPLEMENT_INTERFACE (E_DBUS_SUBPROCESS_TYPE_OBJECT, e_dbus_subprocess_object_proxy__e_dbus_subprocess_object_iface_init)
                         G_IMPLEMENT_INTERFACE (G_TYPE_DBUS_OBJECT, e_dbus_subprocess_object_proxy__g_dbus_object_iface_init))

static void
e_dbus_subprocess_object_proxy_init (EDBusSubprocessObjectProxy *object G_GNUC_UNUSED)
{
}

static void
e_dbus_subprocess_object_proxy_set_property (GObject      *gobject,
  guint         prop_id,
  const GValue *value G_GNUC_UNUSED,
  GParamSpec   *pspec)
{
  G_OBJECT_WARN_INVALID_PROPERTY_ID (gobject, prop_id, pspec);
}

static void
e_dbus_subprocess_object_proxy_get_property (GObject      *gobject,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
  EDBusSubprocessObjectProxy *object = E_DBUS_SUBPROCESS_OBJECT_PROXY (gobject);
  GDBusInterface *interface;

  switch (prop_id)
    {
    case 1:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.gnome.evolution.dataserver.Subprocess.Backend");
      g_value_take_object (value, interface);
      break;

    default:
      G_OBJECT_WARN_INVALID_PROPERTY_ID (gobject, prop_id, pspec);
      break;
  }
}

static void
e_dbus_subprocess_object_proxy_class_init (EDBusSubprocessObjectProxyClass *klass)
{
  GObjectClass *gobject_class = G_OBJECT_CLASS (klass);

  gobject_class->set_property = e_dbus_subprocess_object_proxy_set_property;
  gobject_class->get_property = e_dbus_subprocess_object_proxy_get_property;

  g_object_class_override_property (gobject_class, 1, "backend");
}

/**
 * e_dbus_subprocess_object_proxy_new:
 * @connection: A #GDBusConnection.
 * @object_path: An object path.
 *
 * Creates a new proxy object.
 *
 * Returns: (transfer full): The proxy object.
 */
EDBusSubprocessObjectProxy *
e_dbus_subprocess_object_proxy_new (GDBusConnection *connection,
  const gchar *object_path)
{
  g_return_val_if_fail (G_IS_DBUS_CONNECTION (connection), NULL);
  g_return_val_if_fail (g_variant_is_object_path (object_path), NULL);
  return E_DBUS_SUBPROCESS_OBJECT_PROXY (g_object_new (E_DBUS_SUBPROCESS_TYPE_OBJECT_PROXY, "g-connection", connection, "g-object-path", object_path, NULL));
}

/**
 * EDBusSubprocessObjectSkeleton:
 *
 * The #EDBusSubprocessObjectSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * EDBusSubprocessObjectSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #EDBusSubprocessObjectSkeleton.
 */

static void
e_dbus_subprocess_object_skeleton__e_dbus_subprocess_object_iface_init (EDBusSubprocessObjectIface *iface G_GNUC_UNUSED)
{
}


static void
e_dbus_subprocess_object_skeleton__g_dbus_object_iface_init (GDBusObjectIface *iface)
{
  iface->interface_added = e_dbus_subprocess_object_notify;
  iface->interface_removed = e_dbus_subprocess_object_notify;
}

G_DEFINE_TYPE_WITH_CODE (EDBusSubprocessObjectSkeleton, e_dbus_subprocess_object_skeleton, G_TYPE_DBUS_OBJECT_SKELETON,
                         G_IMPLEMENT_INTERFACE (E_DBUS_SUBPROCESS_TYPE_OBJECT, e_dbus_subprocess_object_skeleton__e_dbus_subprocess_object_iface_init)
                         G_IMPLEMENT_INTERFACE (G_TYPE_DBUS_OBJECT, e_dbus_subprocess_object_skeleton__g_dbus_object_iface_init))

static void
e_dbus_subprocess_object_skeleton_init (EDBusSubprocessObjectSkeleton *object G_GNUC_UNUSED)
{
}

static void
e_dbus_subprocess_object_skeleton_set_property (GObject      *gobject,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  EDBusSubprocessObjectSkeleton *object = E_DBUS_SUBPROCESS_OBJECT_SKELETON (gobject);
  GDBusInterfaceSkeleton *interface;

  switch (prop_id)
    {
    case 1:
      interface = g_value_get_object (value);
      if (interface != NULL)
        {
          g_warn_if_fail (E_DBUS_SUBPROCESS_IS_BACKEND (interface));
          g_dbus_object_skeleton_add_interface (G_DBUS_OBJECT_SKELETON (object), interface);
        }
      else
        {
          g_dbus_object_skeleton_remove_interface_by_name (G_DBUS_OBJECT_SKELETON (object), "org.gnome.evolution.dataserver.Subprocess.Backend");
        }
      break;

    default:
      G_OBJECT_WARN_INVALID_PROPERTY_ID (gobject, prop_id, pspec);
      break;
  }
}

static void
e_dbus_subprocess_object_skeleton_get_property (GObject      *gobject,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
  EDBusSubprocessObjectSkeleton *object = E_DBUS_SUBPROCESS_OBJECT_SKELETON (gobject);
  GDBusInterface *interface;

  switch (prop_id)
    {
    case 1:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.gnome.evolution.dataserver.Subprocess.Backend");
      g_value_take_object (value, interface);
      break;

    default:
      G_OBJECT_WARN_INVALID_PROPERTY_ID (gobject, prop_id, pspec);
      break;
  }
}

static void
e_dbus_subprocess_object_skeleton_class_init (EDBusSubprocessObjectSkeletonClass *klass)
{
  GObjectClass *gobject_class = G_OBJECT_CLASS (klass);

  gobject_class->set_property = e_dbus_subprocess_object_skeleton_set_property;
  gobject_class->get_property = e_dbus_subprocess_object_skeleton_get_property;

  g_object_class_override_property (gobject_class, 1, "backend");
}

/**
 * e_dbus_subprocess_object_skeleton_new:
 * @object_path: An object path.
 *
 * Creates a new skeleton object.
 *
 * Returns: (transfer full): The skeleton object.
 */
EDBusSubprocessObjectSkeleton *
e_dbus_subprocess_object_skeleton_new (const gchar *object_path)
{
  g_return_val_if_fail (g_variant_is_object_path (object_path), NULL);
  return E_DBUS_SUBPROCESS_OBJECT_SKELETON (g_object_new (E_DBUS_SUBPROCESS_TYPE_OBJECT_SKELETON, "g-object-path", object_path, NULL));
}

/**
 * e_dbus_subprocess_object_skeleton_set_backend:
 * @object: A #EDBusSubprocessObjectSkeleton.
 * @interface_: (nullable): A #EDBusSubprocessBackend or %NULL to clear the interface.
 *
 * Sets the #EDBusSubprocessBackend instance for the D-Bus interface <link linkend="gdbus-interface-org-gnome-evolution-dataserver-Subprocess-Backend.top_of_page">org.gnome.evolution.dataserver.Subprocess.Backend</link> on @object.
 *
 * Since: 3.16
 */
void e_dbus_subprocess_object_skeleton_set_backend (EDBusSubprocessObjectSkeleton *object, EDBusSubprocessBackend *interface_)
{
  g_object_set (G_OBJECT (object), "backend", interface_, NULL);
}


/* ------------------------------------------------------------------------
 * Code for ObjectManager client
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:EDBusSubprocessObjectManagerClient
 * @title: EDBusSubprocessObjectManagerClient
 * @short_description: Generated GDBusObjectManagerClient type
 *
 * This section contains a #GDBusObjectManagerClient that uses e_dbus_subprocess_object_manager_client_get_proxy_type() as the #GDBusProxyTypeFunc.
 */

/**
 * EDBusSubprocessObjectManagerClient:
 *
 * The #EDBusSubprocessObjectManagerClient structure contains only private data and should only be accessed using the provided API.
 */

/**
 * EDBusSubprocessObjectManagerClientClass:
 * @parent_class: The parent class.
 *
 * Class structure for #EDBusSubprocessObjectManagerClient.
 */

G_DEFINE_TYPE (EDBusSubprocessObjectManagerClient, e_dbus_subprocess_object_manager_client, G_TYPE_DBUS_OBJECT_MANAGER_CLIENT)

static void
e_dbus_subprocess_object_manager_client_init (EDBusSubprocessObjectManagerClient *manager G_GNUC_UNUSED)
{
}

static void
e_dbus_subprocess_object_manager_client_class_init (EDBusSubprocessObjectManagerClientClass *klass G_GNUC_UNUSED)
{
}

/**
 * e_dbus_subprocess_object_manager_client_get_proxy_type:
 * @manager: A #GDBusObjectManagerClient.
 * @object_path: The object path of the remote object (unused).
 * @interface_name: (nullable): Interface name of the remote object or %NULL to get the object proxy #GType.
 * @user_data: User data (unused).
 *
 * A #GDBusProxyTypeFunc that maps @interface_name to the generated #GDBusObjectProxy derived and #GDBusProxy derived types.
 *
 * Returns: A #GDBusProxy derived #GType if @interface_name is not %NULL, otherwise the #GType for #EDBusSubprocessObjectProxy.
 */
GType
e_dbus_subprocess_object_manager_client_get_proxy_type (GDBusObjectManagerClient *manager G_GNUC_UNUSED, const gchar *object_path G_GNUC_UNUSED, const gchar *interface_name, gpointer user_data G_GNUC_UNUSED)
{
  static gsize once_init_value = 0;
  static GHashTable *lookup_hash;
  GType ret;

  if (interface_name == NULL)
    return E_DBUS_SUBPROCESS_TYPE_OBJECT_PROXY;
  if (g_once_init_enter (&once_init_value))
    {
      lookup_hash = g_hash_table_new (g_str_hash, g_str_equal);
      g_hash_table_insert (lookup_hash, (gpointer) "org.gnome.evolution.dataserver.Subprocess.Backend", GSIZE_TO_POINTER (E_DBUS_SUBPROCESS_TYPE_BACKEND_PROXY));
      g_once_init_leave (&once_init_value, 1);
    }
  ret = (GType) GPOINTER_TO_SIZE (g_hash_table_lookup (lookup_hash, interface_name));
  if (ret == (GType) 0)
    ret = G_TYPE_DBUS_PROXY;
  return ret;
}

/**
 * e_dbus_subprocess_object_manager_client_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusObjectManagerClientFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates #GDBusObjectManagerClient using e_dbus_subprocess_object_manager_client_get_proxy_type() as the #GDBusProxyTypeFunc. See g_dbus_object_manager_client_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call e_dbus_subprocess_object_manager_client_new_finish() to get the result of the operation.
 *
 * See e_dbus_subprocess_object_manager_client_new_sync() for the synchronous, blocking version of this constructor.
 */
void
e_dbus_subprocess_object_manager_client_new (
    GDBusConnection        *connection,
    GDBusObjectManagerClientFlags  flags,
    const gchar            *name,
    const gchar            *object_path,
    GCancellable           *cancellable,
    GAsyncReadyCallback     callback,
    gpointer                user_data)
{
  g_async_initable_new_async (E_DBUS_SUBPROCESS_TYPE_OBJECT_MANAGER_CLIENT, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "flags", flags, "name", name, "connection", connection, "object-path", object_path, "get-proxy-type-func", e_dbus_subprocess_object_manager_client_get_proxy_type, NULL);
}

/**
 * e_dbus_subprocess_object_manager_client_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to e_dbus_subprocess_object_manager_client_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with e_dbus_subprocess_object_manager_client_new().
 *
 * Returns: (transfer full) (type EDBusSubprocessObjectManagerClient): The constructed object manager client or %NULL if @error is set.
 */
GDBusObjectManager *
e_dbus_subprocess_object_manager_client_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return G_DBUS_OBJECT_MANAGER (ret);
  else
    return NULL;
}

/**
 * e_dbus_subprocess_object_manager_client_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusObjectManagerClientFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates #GDBusObjectManagerClient using e_dbus_subprocess_object_manager_client_get_proxy_type() as the #GDBusProxyTypeFunc. See g_dbus_object_manager_client_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See e_dbus_subprocess_object_manager_client_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type EDBusSubprocessObjectManagerClient): The constructed object manager client or %NULL if @error is set.
 */
GDBusObjectManager *
e_dbus_subprocess_object_manager_client_new_sync (
    GDBusConnection        *connection,
    GDBusObjectManagerClientFlags  flags,
    const gchar            *name,
    const gchar            *object_path,
    GCancellable           *cancellable,
    GError                **error)
{
  GInitable *ret;
  ret = g_initable_new (E_DBUS_SUBPROCESS_TYPE_OBJECT_MANAGER_CLIENT, cancellable, error, "flags", flags, "name", name, "connection", connection, "object-path", object_path, "get-proxy-type-func", e_dbus_subprocess_object_manager_client_get_proxy_type, NULL);
  if (ret != NULL)
    return G_DBUS_OBJECT_MANAGER (ret);
  else
    return NULL;
}


/**
 * e_dbus_subprocess_object_manager_client_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusObjectManagerClientFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like e_dbus_subprocess_object_manager_client_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call e_dbus_subprocess_object_manager_client_new_for_bus_finish() to get the result of the operation.
 *
 * See e_dbus_subprocess_object_manager_client_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
e_dbus_subprocess_object_manager_client_new_for_bus (
    GBusType                bus_type,
    GDBusObjectManagerClientFlags  flags,
    const gchar            *name,
    const gchar            *object_path,
    GCancellable           *cancellable,
    GAsyncReadyCallback     callback,
    gpointer                user_data)
{
  g_async_initable_new_async (E_DBUS_SUBPROCESS_TYPE_OBJECT_MANAGER_CLIENT, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "flags", flags, "name", name, "bus-type", bus_type, "object-path", object_path, "get-proxy-type-func", e_dbus_subprocess_object_manager_client_get_proxy_type, NULL);
}

/**
 * e_dbus_subprocess_object_manager_client_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to e_dbus_subprocess_object_manager_client_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with e_dbus_subprocess_object_manager_client_new_for_bus().
 *
 * Returns: (transfer full) (type EDBusSubprocessObjectManagerClient): The constructed object manager client or %NULL if @error is set.
 */
GDBusObjectManager *
e_dbus_subprocess_object_manager_client_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return G_DBUS_OBJECT_MANAGER (ret);
  else
    return NULL;
}

/**
 * e_dbus_subprocess_object_manager_client_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusObjectManagerClientFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like e_dbus_subprocess_object_manager_client_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See e_dbus_subprocess_object_manager_client_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type EDBusSubprocessObjectManagerClient): The constructed object manager client or %NULL if @error is set.
 */
GDBusObjectManager *
e_dbus_subprocess_object_manager_client_new_for_bus_sync (
    GBusType                bus_type,
    GDBusObjectManagerClientFlags  flags,
    const gchar            *name,
    const gchar            *object_path,
    GCancellable           *cancellable,
    GError                **error)
{
  GInitable *ret;
  ret = g_initable_new (E_DBUS_SUBPROCESS_TYPE_OBJECT_MANAGER_CLIENT, cancellable, error, "flags", flags, "name", name, "bus-type", bus_type, "object-path", object_path, "get-proxy-type-func", e_dbus_subprocess_object_manager_client_get_proxy_type, NULL);
  if (ret != NULL)
    return G_DBUS_OBJECT_MANAGER (ret);
  else
    return NULL;
}


